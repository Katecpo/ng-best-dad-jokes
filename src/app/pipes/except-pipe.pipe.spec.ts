import { ExceptPipePipe } from './except-pipe.pipe';

describe('ExceptPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExceptPipePipe();
    expect(pipe).toBeTruthy();
  });
});
